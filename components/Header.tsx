import React from 'react';
import Heading from './Heading';

const Header = () => {
    return (
        <header>
            <Heading title='header' />
        </header>
    );
};

export default Header;
