import React from 'react';
import Heading from './Heading';
import NavBar from './NavBar';

const Header = () => {
    return (
        <header>
            <NavBar />
        </header>
    );
};

export default Header;
